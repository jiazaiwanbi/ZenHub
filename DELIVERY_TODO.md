# ZenHub 交付 TODO

这个清单面向“把当前项目从可试用 Demo 推进到可长期交付的成熟成品”。

当前状态判断：

- 本地代理、`direct` / `relay`、配置同步、冲突处理、托盘入口已经有功能闭环
- `go build ./cmd/client ./cmd/gui ./cmd/server-community` 可通过
- `go test ./...` 可通过
- 但还缺少交付级别的安装、发布、恢复、文档、运维和手工验收

---

## P0：交付前必须完成

### 1. 启动与安装

- [ ] 明确最终用户入口，只保留一个正式客户端启动方式
- [ ] 提供 macOS / Windows 的可分发构建产物
- [x] 提供默认配置目录规范
  - [x] 配置文件放哪里
  - [x] 同步状态文件放哪里
  - [x] 日志文件放哪里
- [x] 提供首次启动最小配置向导或默认配置模板
- [ ] 验证“空配置 / 缺配置 / 配置损坏”时的错误提示是否对普通用户可理解

### 2. 托盘模式稳定性

- [ ] 手工验证 macOS 托盘图标、右键菜单、退出行为
- [ ] 手工验证 Windows 托盘图标、右键菜单、退出行为
- [ ] 验证托盘模式下应用关闭时一定会执行 `Shutdown`
- [ ] 验证冲突状态下托盘菜单项的启用/禁用是否正确
- [ ] 验证 `Sync Now` 连点、重复点击、超时、网络断开时不会卡死

### 3. 配置与数据安全

- [ ] 不在默认日志中打印明文密码、token、API key
- [ ] 检查配置文件权限
  - [x] macOS / Linux 默认 `0600`
  - [ ] Windows 行为可接受
- [x] 明确 `api_key` 与 `api_key_env` 的优先级和文档说明
- [x] 明确 sync 用户名/密码的推荐存放方式
- [x] 配置保存失败时要么完全回滚，要么明确提示“未生效”

### 4. 故障恢复

- [x] 验证服务端不可达时客户端仍能本地启动
- [x] 验证首次同步冲突时不会破坏本地配置
- [x] 验证退出上传失败时不会丢失本地配置
- [x] 验证同步状态文件损坏时的恢复路径
- [ ] 验证上游超时、5xx、认证失败时请求错误信息可追踪

### 5. 交付文档

- [x] 补一个根目录 `README.md`
- [x] 写清楚项目定位、支持范围、非目标
- [x] 写清楚客户端启动方式
- [x] 写清楚服务端启动方式
- [x] 写清楚最小可运行示例
- [ ] 写清楚 `sample-config.json` 每个字段的含义
- [x] 写清楚同步冲突出现后如何处理

---

## P1：建议在第一次对外试用前完成

### 6. 发布工程化

- [ ] 加 CI
  - [ ] `go test ./...`
  - [ ] `go build` 主要入口
  - [ ] `gofmt` / `go vet`
- [ ] 固定 Go 版本和构建方式
- [ ] 提供 release 构建脚本
- [ ] 产出版本号、构建时间、commit hash

### 7. 可观测性

- [ ] 增加应用级日志初始化
- [ ] 区分 info / warn / error
- [ ] 为同步、relay、direct、配置加载增加关键日志
- [ ] 提供导出诊断信息的方法
  - [ ] 当前配置路径
  - [ ] 当前监听地址
  - [ ] 当前同步状态
  - [ ] 最近错误

### 8. 配置编辑体验

- [ ] 决定正式交付时是否保留 raw JSON 编辑
- [ ] 结构化编辑器补手工验收
  - [ ] 路由新增/删除
  - [ ] provider group 改名联动
  - [ ] node 新增/删除
  - [ ] 错误输入后的提示
- [ ] 对结构化编辑增加更明确的成功/失败反馈
- [ ] 防止用户编辑期间被外部配置覆盖而无提示

### 9. 服务端可用性

- [ ] 明确社区服务端的部署方式
  - [ ] 本地单机
  - [ ] MySQL 配置
  - [ ] 初始账号
- [ ] 补最小部署文档
- [ ] 补健康检查接口或部署检查命令
- [ ] 验证 relay 和 sync 在真实部署环境下可连通

---

## P2：做完会更像成熟产品

### 10. 账号与凭据管理

- [ ] 不再要求用户手改 JSON 来填同步账号
- [ ] 提供本地凭据管理方案
- [ ] 支持修改密码/token 后热更新

### 11. 用户体验

- [ ] 托盘菜单显示更友好的状态文案
- [ ] 增加“打开日志目录 / 打开配置目录”
- [ ] 增加“复制监听地址”
- [ ] 增加“最近同步结果”的更短摘要
- [ ] 增加首次启动引导

### 12. 数据迁移与兼容

- [ ] 定义配置文件版本字段
- [ ] 为未来字段调整预留迁移逻辑
- [ ] 验证旧配置升级到新版本的路径

### 13. 更全面测试

- [ ] 增加端到端测试
  - [ ] client + server-community + upstream
  - [ ] direct 请求
  - [ ] relay 请求
  - [ ] pull / push / conflict
- [ ] 增加托盘相关的最小行为测试
- [ ] 增加配置损坏与恢复测试

---

## P3：后续产品化方向

### 14. 平台能力

- [ ] 自启动
- [ ] 自动更新
- [ ] 崩溃恢复
- [ ] 托盘通知

### 15. 协议与产品能力

- [ ] Claude / Gemini / Codex 协议扩展
- [ ] 更多模型目录能力
- [ ] 更完整的服务端账号体系
- [ ] 闭源中心服务相关能力

---

## 建议推进顺序

1. 先做 `README + 安装方式 + 配置目录约定`
2. 再做 `托盘手工验收 + 故障恢复验收`
3. 然后补 `日志/CI/release 构建`
4. 最后再决定是否继续做更完整的配置编辑和账号体验

## 可以作为“已达到可交付”的最低标准

满足下面这些后，才比较接近“可以交给别人长期使用”：

- [ ] 双平台可启动并有正式构建产物
- [ ] 托盘模式手工验收通过
- [ ] 同步失败和冲突场景手工验收通过
- [ ] 无敏感信息泄漏到日志
- [ ] README 和最小部署文档完整
- [ ] CI 持续验证 build + test
- [ ] 至少完成一轮真实机器上的端到端联调
